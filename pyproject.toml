[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "clarev"
version = "0.1.0"
description = "ClareV experimental package"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
include = ["clarev*"]
